@{
    // Note that if this file was place in Home folder it would have only set the layout view for the file inside the Home folder.

    // You could overwrite this variable by putting it inside  the cshtml files 
    
    // Usuall responsibility of this file is to set the layout view 
    Layout = "~/Views/Shared/_Layout.cshtml";
}
